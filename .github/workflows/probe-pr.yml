name: probe
# evidence-trigger-v2

# Validates the benchmark itself. Runs on every change to a probe, to the
# harness, or to a reference model, because any of the three can silently
# break the acceptance gate.
#
# Read-only token and no secrets: a probe is arbitrary Python from a
# contributor and must never see a credential.

on:
  pull_request:
    paths:
      - 'probes/**'
      - 'src/**'
      - 'schemas/**'
      - 'models/**'
      - '.gitattributes'
      - '.github/workflows/probe-pr.yml'
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install
        run: pip install -e '.[dev]'

      - name: Data budget
        # Probes ship a seeded generator, not a dataset. Anything large that
        # appears under probes/ is a mistake, and it is much cheaper to catch
        # here than after it is in the history.
        run: |
          oversized=$(find probes -type f -size +1M -not -name '*.md' || true)
          if [ -n "$oversized" ]; then
            echo "::error::probes must ship a generator, not data. Oversized files:"
            echo "$oversized"
            exit 1
          fi
          echo "no committed probe data over 1 MB"

      - name: Validate specs
        run: ht validate

      - name: Harness tests
        run: pytest -q

      - name: Acceptance gate
        # The load-bearing step. Every probe must pass the physically exact
        # reference model and must fail each deliberately broken one, on the
        # specific criterion declared to do the catching.
        run: ht gate

  container:
    # The reference models run via subprocess so the gate stays fast, which
    # means the container path would otherwise go untested until a real
    # submission arrived. GitHub runners have Docker, so exercise it here.
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install
        run: pip install -e '.[dev]'

      - name: Run a known-good model through the container path
        run: |
          ht run --model reference_bucket \
                 --probe mass/catchment-closure \
                 --seed 4242 \
                 --runner docker


  archive_evidence:
    # Temporary CI-only job. This branch is never merged.
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install
        run: pip install -e '.[dev]'

      - name: Generate exact archive rows
        run: |
          set +e
          rm -f /tmp/lisflood-full.csv /tmp/wave.csv

          ht run --model lisflood --gate-seeds --csv /tmp/lisflood-full.csv             > /tmp/lisflood-full.txt 2>&1
          lisflood_rc=$?

          for model in cwatm dhbv2 flex_lumped flex_topo google_flood_forecast modflow6 sacsma_snow17 summa wflow_sbm; do
            ht run --model "$model"               --probe momentum/wave-celerity-bounds               --gate-seeds               --csv /tmp/wave.csv               > "/tmp/${model}-wave.txt" 2>&1
            echo "$model rc=$?"
          done

          echo "LISFLOOD_RC=$lisflood_rc"
          echo "LISFLOOD_REPORT_BEGIN"
          cat /tmp/lisflood-full.txt
          echo "LISFLOOD_REPORT_END"
          echo "ARCHIVE_LISFLOOD_FULL_BEGIN"
          cat /tmp/lisflood-full.csv
          echo "ARCHIVE_LISFLOOD_FULL_END"
          echo "ARCHIVE_WAVE_BEGIN"
          cat /tmp/wave.csv
          echo "ARCHIVE_WAVE_END"
          exit 0
